[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: ทุก path ที่ไม่ตรงกับไฟล์จริง ให้ส่งกลับ index.html
# จำเป็นสำหรับ React Router เหมือนกับที่ทำใน frontend/vercel.json และ frontend/nginx.conf
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
